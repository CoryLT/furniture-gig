# FlipWork — Session Handoff (2026-05-28)

Quick catch-up doc for the next session. Everything below is already
committed and pushed to `main` unless flagged otherwise.

---

## What we did this session

1. **Hamburger menu reorganized** — links sorted into logical groups
   (Gigs / Flipper / Marketplace / Connect) with divider lines between
   them instead of one flat list. (`components/shared/Nav.tsx`)

2. **Dashboard (`/home`) turned into a navigation hub**
   - Dropped the vanity stat tiles (incl. the misleading "Total earned /
     Total invested" — they ignored marketplace cash sales).
   - Dropped the "You vs. the community" section.
   - Kept the action cards (review needed, applicants, unread, in-progress)
     up top; a lone card now stretches full width.
   - The 30-day chart now only shows if the user has earned OR invested
     real gig money (already renders earned/invested in two colors).
   - Added a grouped "Go to" navigation grid covering every destination,
     placed above the chart. ("Services I offer" lives under Marketplace.)
   - File: `app/home/page.tsx`

3. **Favicon added** — amber rounded-square with the white armchair logo.
   `app/icon.png` (tab) + `app/apple-icon.png` (home screen). Next.js
   auto-detects these; no metadata change needed.

4. **Services tab in the marketplace**
   - New "Items | Services" toggle above the marketplace toolbar.
   - Services come from `worker_services`; city filter matches the
     PROVIDER's profile city (services have no city of their own).
   - Tapping a service card → that provider's profile (`/u/[username]`).
   - In services mode the "Free only" + price-sort controls are hidden.
   - Files: `app/marketplace/page.tsx`, `app/marketplace/MarketplaceFeed.tsx`,
     `app/marketplace/ServiceCard.tsx`

5. **Cover image on services** (one photo per service)
   - SQL adds `image_path` to `worker_services` (see below — needs running
     in Supabase if not already done).
   - Upload route `app/api/upload-service-image/route.ts` — reuses the
     existing `marketplace-photos` storage bucket + image moderation.
   - Editor: photo picker / change / remove on each service row at
     `/profile/worker/services`.
   - Display: photo shows as a banner on the marketplace service card;
     a hammer icon is the fallback when there's no photo.
   - Note: image is added by EDITING an existing service (the upload needs
     a service ID, which only exists after the service is created).

6. **Public profile** — "Back to gigs" link changed to "Back to dashboard"
   (`/home`) for logged-in users. (`components/profile/PublicProfileClient.tsx`)

7. **My Posted Gigs (`/flipper/dashboard`)** — removed the whole row of
   stat/filter tiles. The gig list below already has its own filter
   buttons, so no filtering was lost. Also removed the now-dead payout
   query that only fed the "Paid Out" tile.

8. **Message icon + dropdown** — new envelope icon in the nav (left of the
   notification bell) with an unread count badge and a dropdown listing
   each unread conversation (sender + preview), tap to open the chat.
   Opening a chat marks it read and clears it. Covers gig + listing
   messages. (`components/shared/MessageBell.tsx`, wired in `Nav.tsx`)

---

## ACTION REQUIRED — run this SQL in Supabase

If not already done, the service-image feature needs this. Supabase →
SQL Editor → New query → paste → Run:

```sql
alter table public.worker_services
  add column if not exists image_path text;
```

(Saved as `supabase/schema_worker_service_image_20260528.sql`.)

---

## Open items / things to verify next time

- **Test the service image upload live.** Built and compiles, but couldn't
  be tested here (needs the live Supabase connection). Most likely snag if
  any: storage permissions on the `marketplace-photos` bucket.
- **Test the message bell live.** Same — realtime behavior wasn't testable
  in the sandbox. Verify the count and dropdown update correctly.
- **Redundant unread badges.** The hamburger menu still shows its own
  unread-message badge from before the new message icon existed. Harmless
  but duplicate — can be removed so there's just one message indicator.
- **Dead leftover code (optional cleanup):**
  - `app/flipper/dashboard/FilterTile.tsx` — no longer imported anywhere.
  - `app/home/page.tsx` still has the "You vs. community" percentile
    CALCULATIONS running even though nothing renders them. Harmless, can
    be stripped.
  - `app/marketplace/page.tsx` has a leftover `console.log` "TEMP
    DIAGNOSTIC" about blank photo thumbnails. Remove if that bug is solved.

---

## NEXT UP (not started): PayPal → Stripe cleanup

Decided this session but deferred: Stripe is the real payment plan now;
PayPal references are stale leftovers and should be removed.

- Clean up on-screen wording AND the database, but don't remove anything a
  user would notice missing (e.g. don't drop a profile field, leaving it
  blank for no reason).
- Do NOT drop the `paypal_email` column outright — live pages still use it
  (worker profile editor, onboarding, admin review). Safe cleanup = stop
  requiring/collecting it in the UI and remove references, leave the dead
  column for a later dedicated pass.
- PayPal appears in (from a grep this session): `types/database.ts`,
  `supabase/schema.sql` (agreement text line), `app/profile/worker/page.tsx`,
  `app/admin/review/[claimId]/page.tsx`, `lib/support-prompt.ts`,
  `lib/validations/profile.ts`, `app/page.tsx`, plus several `.md` docs.
- Tackle one file at a time, test, commit — per the usual workflow.

---

## Working notes / reminders

- Claude does the coding; Cory doesn't copy/paste code (one SQL snippet
  this session was the exception, since Supabase changes can't be done for
  him).
- One file at a time, test, commit, move on.
- To deploy: in VS Code terminal, `git pull` then `git push` (the push is
  what triggers Vercel; it does not auto-deploy).
- `next.config.js` has `ignoreBuildErrors: true` + `ignoreDuringBuilds:
  true`, so TS/ESLint errors won't block the Vercel build — but a real
  `next build` still catches genuine syntax errors, which is the useful
  pre-push check.
